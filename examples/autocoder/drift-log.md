# Drift Log

Append-only record of implementation discoveries (spec §10). Each entry records the
discovery, the affected layer (derived vs. requirement), the action taken, and the
escalation status.

Format:

```
## DRIFT-NNN  (SLICE-x / TASK-yyy, Builder)  — <layer>, <action>
Discovery : ...
Action    : ...
Escalation: ...
```
## DRIFT-001  (SLICE-0 / TASK-001, Builder)  — derived layer, auto-applied
Discovery : 07-contracts.md IF-017 Config pins {apiKey,modelId,root,editMode,commandMode,maxIterations,tokenBudget,allowlist} and treats the Task as AgentRun.task (IF-014 CLI positional), not a Config field. The walking skeleton needs a thin Config and threads the task through it.
Action    : Built a minimal Slice-0 Config type {apiKey,modelId,root,task} carrying task for the skeleton; the editMode/commandMode/budget/allowlist fields are deferred to SLICE-1 (real config precedence + fail-fast). No production deviation since those fields are unused this slice. Flagged here; the full IF-017 shape will be realized in SLICE-1/TASK-003 — the contract itself is unchanged (no field removed/retyped).
Escalation: none (no requirement contradicted).
## DRIFT-002  (SLICE-1 / TASK-003, Builder)  — derived layer, auto-applied
Discovery : DRIFT-001 deferral completion: SLICE-0 pinned a minimal Config {apiKey,modelId,root,task}; IF-017 (07-contracts.md) specifies the full schema. This task realizes the full IF-017 Config type. Two contract-descriptive defaults needed concrete literals not pinned in the doc: the 'current Claude model' default and the 'safe read-only commands' default allowlist.
Action    : Expanded src/contracts.ts Config to the full IF-017 shape {apiKey,modelId,root,editMode,commandMode,maxIterations,tokenBudget,allowlist,task}; implemented precedence flags>env>file>defaults in src/config.ts. Pinned DEFAULT_MODEL='claude-sonnet-latest' and DEFAULT_ALLOWLIST=[git status, git diff, ls, cat] as the concrete realizations of the doc's descriptive defaults. No contract field removed/retyped; code now conforms to the existing IF-017 doc (no doc edit required). apiKey kept env-only and never serialized (redaction assertion is SLICE-8).
Escalation: none (no requirement contradicted).
## DRIFT-003  (SLICE-2 / TASK-005, Builder)  — derived layer, auto-applied
Discovery : SLICE-0 walking-skeleton test asserted an exact 4-event transcript ([run-started, tool-called, tool-result, run-completed]) and seqs [0,1,2,3]. The now-real loop legitimately emits more entries per IF-015 / technical-design steps 2 and 4: a context-gathered entry (REQ-003) and a per-turn iteration-started entry. The skeleton test's exact-equality assertion was stale (it encoded the skeleton-only transcript shape).
Action    : Wired the real loop to emit context-gathered + iteration-started (matches existing technical-design steps 2/4 and IF-015 — design doc already specified these, no design edit needed). Relaxed the SLICE-0 test from exact-equality to assert the ordered subsequence (run-started -> context-gathered -> tool-called -> tool-result -> run-completed) plus monotonic gap-free seq, preserving the REQ-NFR-002 determinism contract it anchors.
Escalation: none (no requirement contradicted).
## DRIFT-004  (SLICE-2 / TASK-004, Builder)  — derived layer, auto-applied
Discovery : TASK-004 + the Assumptions say the detected test command is 'overridable via config', but the resolved Config (IF-017 / contracts.ts) has NO testCommand field. config.ts is OUTSIDE my SLICE-2 component boundary (config/cli/path-sandbox are off-limits), so I cannot add a Config.testCommand field here without a component overlap.
Action    : Implemented the override at the repo-context seam: buildRepoContext(root, { testCommandOverride }) — a non-empty override wins over package.json detection (asserted by test_REQ003_context_lists_and_detects_testcmd). This satisfies 'overridable via config' structurally; threading a real Config.testCommand field into the override is deferred to the config-owning slice (no config.ts edit made). cli.ts wiring still calls buildRepoContext(root) with detection only.
Escalation: none (no requirement contradicted).
## DRIFT-005  (SLICE-3 / TASK-008, Builder)  — derived layer, auto-applied
Discovery : TASK-008 instructs wiring list_search into tool-registry dispatch, but 09-implementation-plan SLICE-3 'Components touched' lists only {tool-read, tool-search, path-sandbox} (tool-registry is SLICE-2-owned per build order). The live composition root cli.ts (out of SLICE-3 boundary, in the do-not-modify set) still constructs the registry with one arg, so the live loop would use the list_search STUB even though the real tool exists.
Action    : Made createToolRegistry accept an OPTIONAL searchTool (defaults to the existing stub) so the registry seam wires the real list_search without breaking SLICE-2 single-arg call sites; updated the tool-registry.ts header comment to document the SLICE-3 real-body wiring. Left cli.ts UNTOUCHED (out of boundary). The one-line composition wiring (passing createSearchTool(sandbox) to createToolRegistry in cli.ts) is deferred to the cli/tool-registry owner; SLICE-3 acceptance tests exercise the real tools directly and via the registry's new param. No code outside {tool-read, tool-search, path-sandbox, tool-registry dispatch table} changed.
Escalation: none (no requirement contradicted).
## DRIFT-006  (SLICE-3 / TASK-008, Builder)  — derived layer, auto-applied
Discovery : The SLICE-2 placeholder list_search JSON schema in tool-registry.ts exposed only {path (required), query} — it did not match IF-002, which requires 'mode' (enum list|search) and makes 'path' OPTIONAL (default '.'), plus glob/isRegex/maxResults. A model calling the real tool per IF-002 (mode-driven, path-optional) would have mismatched the advertised schema.
Action    : Updated the list_search inputSchema in tool-registry.ts to IF-002: properties {mode(enum), path, glob, query(minLen1), isRegex(bool), maxResults(1..2000)} with required:['mode']; kept exactly the five schemas. This is a contract-conformance fix, not new behavior; IF-002 in 07-contracts.md already specifies these fields so no doc edit was needed (the code was the stale side).
Escalation: none (no requirement contradicted).
## DRIFT-007  (SLICE-4 / TASK-009, Builder)  — derived layer, auto-applied
Discovery : IF-009 ApprovalGate.resolveEdit was declared synchronous (resolveEdit(edit:unknown,policy:unknown):ApprovalDecision) but REQ-012 confirm-each needs an injectable async prompt seam; the contract also lacked typed Edit/EditApprovalPolicy shapes and a clean (non-fatal) user-abort signal carrier, and IF-013 generateDiff's exact output format was unpinned.
Action    : Made ApprovalGate.resolveEdit async (Promise<ApprovalDecision>) with typed Edit + EditApprovalPolicy params in contracts.ts; added Edit/EditApprovalPolicy types; added non-fatal UserAbortError (code USER_ABORT) + isUserAbortError in tool-errors.ts for the clean user-abort StopCondition carrier; pinned the resolveEdit async/confirm-seam/user-abort note and the generateDiff output-format note into docs/07-contracts.md (IF-009, IF-013). cli.ts no-arg createApprovalGate() call unaffected.
Escalation: none (no requirement contradicted).
## DRIFT-008  (SLICE-4 / TASK-010, Builder)  — derived layer, auto-applied
Discovery : The real write_edit executor needs to be reachable for the loop, but cli.ts (composition root) is owned by SLICE-10 and is out of this slice's component boundary; the registry's write_edit JSON schema still carried the SLICE-2 placeholder shape (path/content) instead of the IF-003 shape (targetPath/mode/search/replacement/replaceAll); and the registry catch only re-raised FatalToolError, so a user-abort would have been wrongly normalized to an error ToolResult.
Action    : Wired the real write_edit executor into tool-registry.ts via a new optional writeEditTool param (defaulting to the stub preserves earlier call sites) so it is reachable through the dispatch SEAM and asserted by test_REQ008_write_edit_dispatches_via_registry; updated the write_edit schema to the IF-003 shape (still exactly five schemas, RULE-012); added isUserAbortError re-raise in the registry catch so user-abort propagates as a clean stop (Stopped not Failed). The cli.ts composition-root wiring of write_edit is DEFERRED to SLICE-10 (which owns cli + all tools) — cli.ts NOT edited, matching SLICE-3's deferred list_search wiring.
Escalation: none (no requirement contradicted).
## DRIFT-009  (SLICE-5 / TASK-011, Builder)  — derived layer, auto-applied
Discovery : IF-009 contract pinned resolveCommand as SYNC (sync until SLICE-5 builds the allowlist decision) and typed policy/allowlist as unknown. SLICE-5 builds that decision: a non-allowlisted command must PROMPT via the same injectable async confirm seam as resolveEdit, so the realized shape is Promise<ApprovalDecision> with typed CommandApprovalPolicy + CommandAllowlist params.
Action    : Made ApprovalGate.resolveCommand return Promise<ApprovalDecision> with typed params (added CommandApprovalPolicy + CommandAllowlist to contracts.ts); added a confirmCommand async seam to approval-gate.ts; updated the IF-009 prose in docs/07-contracts.md (resolveCommand now async) and the ApprovalGate interface JSDoc in src/contracts.ts to match. NOTE: docs/07-contracts.md edited — Orchestrator should re-register + run cascade.
Escalation: none (no requirement contradicted).
## DRIFT-010  (SLICE-5 / TASK-011, Builder)  — derived layer, auto-applied
Discovery : IF-007 CommandRunner result shape {exitCode,stdout,stderr,timedOut} cannot distinguish a SPAWN FAILURE (process never started → COMMAND_FAILED/ERR-010) from a process that ran and exited non-zero (a valid result, ADR-007). The seam result needs to carry that signal for tool-runcommand to map COMMAND_FAILED vs an ok non-zero-exit result.
Action    : Added an OPTIONAL additive field CommandResult.spawnFailed?:boolean (ADR-002 versioning, back-compat with the SLICE-0 stub) to src/contracts.ts; command-runner sets it true on the spawn 'error' event / sync spawn throw; tool-runcommand maps spawnFailed:true → COMMAND_FAILED and a plain non-zero exit → status:ok. Updated the IF-007 output block in docs/07-contracts.md. NOTE: docs/07-contracts.md edited — Orchestrator should re-register + run cascade.
Escalation: none (no requirement contradicted).
## DRIFT-011  (SLICE-5 / TASK-012, Builder)  — derived layer, auto-applied
Discovery : The real run_command tool (tool-runcommand) is built and wired into tool-registry via the new optional runCommandTool param (4th arg), but the composition root cli.ts still calls createToolRegistry without it, so the live CLI run path still dispatches the run_command STUB. Per the SLICE-5 boundary, cli.ts wiring is deferred to SLICE-10 (composition root), so cli.ts was intentionally NOT edited.
Action    : Left cli.ts unedited (SLICE-5 boundary excludes it). The registry seam (runCommandTool param) is in place; SLICE-10 must construct CommandRunner + Allowlist + RunCommandTool (with workingRoot=Config.root, policy=Config.commandMode, allowlist=Config.allowlist, testCommand=repoContext.testCommand) and pass it as the 4th arg to createToolRegistry. Deferred to SLICE-10.
Escalation: none (no requirement contradicted).
## DRIFT-012  (SLICE-6 / TASK-013, Builder)  — derived layer, auto-applied
Discovery : IF-013 read side (parsePatch/applyHunks) and IF-005 (apply_patch) were under-specified on realization details: parsePatch's failure shape was unpinned (throw vs discriminated), applyHunks' context-match semantics + new-file anchor were unpinned, and IF-005 did not say how the single-approval gate (which takes ONE Edit) represents a multi-file patch. 07-contracts.md also wrongly implied 'stated/fuzzed offset' matching, but the MVP uses EXACT line context (AST/git-aware/fuzzy is explicitly V1, out of scope per the task file).
Action    : Wired to reality: parsePatch returns a discriminated {ok:true,patch}|{ok:false,reason} so the tool maps PATCH_MALFORMED with no try/catch (RULE-008); applyHunks is a pure exact-context dry-run returning {applicable,result?,failedHunkIndex?} with fresh result (no input/disk mutation); apply_patch feeds the gate ONE synthetic patch-Edit whose diff is every per-file diff concatenated for a single approval over the whole blast radius. Updated docs/07-contracts.md: pinned a SLICE-6 realization note under IF-013 (parsePatch discriminated result, Hunk shape, exact-context apply, new-file anchor, tool-enforced atomicity) and under IF-005 (the dry-run-everything-then-write protocol + single-approval representation), and corrected the IF-005 preconditions wording from 'stated/fuzzed offset' to EXACT line context (fuzzed = V1). No contract field removed/retyped. NOTE: docs/07-contracts.md edited — Orchestrator should re-register + run cascade.
Escalation: none (no requirement contradicted).
## DRIFT-013  (SLICE-6 / TASK-013, Builder)  — derived layer, auto-applied
Discovery : The real apply_patch executor (tool-applypatch) is the LAST stub replaced in tool-registry, wired via a new optional applyPatchTool param (5th arg) so it is reachable through the dispatch SEAM (asserted by test_REQ023_apply_patch_dispatches_via_registry). But the composition root cli.ts (owned by SLICE-10, in the do-not-modify set for this slice) still calls createToolRegistry without it, so the live CLI run path still dispatches the apply_patch STUB. This mirrors the deferred wiring of list_search (DRIFT-005), write_edit (DRIFT-008), and run_command (DRIFT-011).
Action    : Left cli.ts UNTOUCHED (out of SLICE-6 boundary). The registry seam (applyPatchTool param) is in place and defaults to the stub, preserving all earlier call sites; all five tools now have real bodies behind the seam. SLICE-10 must construct ApplyPatchTool (sandbox=PathSandbox over Config.root, approval=ApprovalGate, policy=Config.editMode, transcript+runId) and pass it as the 5th arg to createToolRegistry. Deferred to SLICE-10.
Escalation: none (no requirement contradicted).
## DRIFT-014  (SLICE-7 / TASK-014, Builder)  — derived layer, auto-applied
Discovery : src/agent-run.ts carried a TEMPORARY structural turn cap (STRUCTURAL_TURN_CAP=32) placeholder from SLICE-2 to bound the loop until the real ceilings landed; the spec (RULE-006, INV-004) requires a pre-turn budget guard, not a hard-coded cap.
Action    : Replaced the structural cap with the real budget-stop pre-turn guard: agent-run now constructs/consumes a BudgetController (IF-011), checks checkGuard() BEFORE every LlmClient.send (no half-iteration), accrues usage after each send, and emits budget-exceeded when a ceiling is hit. Added OPTIONAL budget/maxIterations/tokenBudget fields to AgentRunDeps (additive, back-compatible; CLI signature untouched). No 07-contracts.md edit needed: IF-011 already specifies this exact guard behavior; implementation now matches it.
Escalation: none (no requirement contradicted).
## DRIFT-015  (SLICE-7 / TASK-015, Builder)  — derived layer, auto-applied
Discovery : src/agent-run.ts terminal path was hard-coded in SLICE-2: task-success went through reporter.renderOutcome and the catch block hard-coded an unrecoverable-error RunOutcome { status:failed, exitCode:1 }; user-abort had no terminal path even though tool-errors.ts (UserAbortError) and IF-011 already specify user-abort as a CLEAN Stopped (not Failed) StopCondition.
Action    : Routed ALL terminal classification through budget-stop.classify(signal) via a single terminate() helper in agent-run: task-success/model-give-up/unrecoverable-error/user-abort each map to exactly one StopCondition + RunOutcome with exitCode==0 IFF succeeded (RULE-011, INV-006), and a hit ceiling takes precedence (single StopCondition, INV-005). The catch block now distinguishes a re-raised UserAbortError (-> user-abort/Stopped, clean) from any other throw (-> unrecoverable-error/Failed). reporter.renderOutcome is no longer the classifier (reporter still in deps; signature untouched). No 07-contracts.md field change: matches IF-011 classify + run-stopped/run-completed exactly.
Escalation: none (no requirement contradicted).
## DRIFT-016  (SLICE-8 / TASK-016, Builder)  — derived layer, auto-applied
Discovery : IF-012 had no TRANSCRIPT_WRITE_FAILED carrier class and the SLICE-0 writer re-opened a fresh file handle per append (not a single persistent writer). Also: ERR-014 says a write failure surfaces to agent-run -> unrecoverable-error -> Failed, but agent-run's pre-loop appends (run-started, context-gathered) are OUTSIDE its run-lifecycle try/catch, so a failure on those two first entries is not classified; agent-run is out of the SLICE-8 boundary (do-not-modify).
Action    : Hardened src/transcript.ts to a single persistent append-mode handle opened once in open() (single writer per run); each append assigns a gap-free seq AFTER a durable write + fsync, advancing seq only on success (no gap on failure); added a TranscriptWriteError class with stable code TRANSCRIPT_WRITE_FAILED + isTranscriptWriteError guard so agent-run's existing catch routes it to unrecoverable-error -> Failed; made readTranscript parse-SKIP a partial last line (crash tolerance, ADR-002) and surface a non-terminal corrupt line. The in-loop fatal path is proven (test_REQ022_transcript_write_fatal fails on an iteration-started append). The pre-loop classification gap is noted for the agent-run owner; not fixed here (out of boundary). No 07-contracts.md field change: code now conforms to IF-012/IF-015/ERR-014 as written (schemaVersion 1.0, 18 types, envelope intact).
Escalation: none (no requirement contradicted).
## DRIFT-017  (SLICE-8 / TASK-017, Builder)  — derived layer, auto-applied
Discovery : IF-016 RunSummary was pinned in 07-contracts.md as a --json object shape but had NO corresponding TypeScript type in src/contracts.ts (only the minimal RunOutcome {status,exitCode,runId} existed), and the SLICE-0 reporter only had renderOutcome — no human stream, no --json, no secret-redaction mechanism. The redaction MECHANISM (how REQ-018 keeps the apiKey out of stdout) was unpinned.
Action    : Added additive types to src/contracts.ts: RunSummary (full IF-016 shape), FileChange, TestsResult, StopConditionName, plus an optional estimated:boolean token flag (ODQ-005) — no existing field removed/retyped, code now conforms to the existing IF-016 doc. Fleshed src/reporter.ts: kept renderOutcome back-compatible; added an injectable redacting stdout writer; added the ordered human stream (streamPlan/streamToolCall/streamToolResult/streamDiff/streamTestResult) and renderSummary that renders ONE classified RunSummary two ways (human always, --json when set) REUSING the SLICE-7 classification (never recomputes status/exitCode/stopCondition); every stdout byte passes through a literal-secret redaction seam (-> [REDACTED]) so the apiKey leaks via no channel. Pinned a SLICE-8 realization note under IF-016 in docs/07-contracts.md describing the compute-once/render-twice + redaction mechanism (additive clarification, no field change). NOTE: docs/07-contracts.md edited — Orchestrator should re-register + run cascade.
Escalation: none (no requirement contradicted).
## DRIFT-018  (SLICE-9 / TASK-018, Builder)  — derived layer, auto-applied
Discovery : IF-015 defines an allowlist-changed TranscriptEntry {op,pattern}, but the allowlist subcommand starts NO agent loop and opens NO transcript (no runId/run context), so there is no per-run audit log to append it to. The architecture secondary flow specifies 'Reporter confirms' for this path.
Action    : Wired the no-loop mutating op to surface the change via reporter.streamAllowlistChanged(op,pattern,changed) carrying the same {op,pattern} data; reserved the allowlist-changed transcript row for any future in-RUN allowlist mutation. Pinned this realization note in 07-contracts.md beside the allowlist-changed payload (IF-015 section).
Escalation: none (no requirement contradicted).
## DRIFT-019  (SLICE-9 / TASK-018, Builder)  — derived layer, auto-applied
Discovery : RULE-014 / IF-017 say allowlist changes 'persist to the config file' but did not pin WHICH file when --config is omitted, nor the FS-write seam needed to test a persistence failure (FAIL-004) deterministically.
Action    : Implemented set-management + write-back in config.ts (mutateAllowlist/inspectAllowlist + AllowlistPersistError + injectable ConfigFsSeam); config-file target = --config path else default .autocoder.json in cwd (resolved absolute); persistence re-writes the whole config object preserving unrelated fields; confirmation printed only after a successful write. allowlist.ts matcher (SLICE-5) untouched. Pinned the config-file-target + failure realization note in 07-contracts.md beside the Config/RULE-014 validation rules.
Escalation: none (no requirement contradicted).
## DRIFT-020  (SLICE-10 / TASK-019, Builder)  — derived layer, auto-applied
Discovery : The composition root (cli.ts composeAndRun) constructed only the read-only registry (createToolRegistry(readTool)) and an unwired ApprovalGate; per DRIFT-005/008/011/013 the four other real tool executors (list_search/write_edit/run_command/apply_patch) existed behind the registry seam but were NEVER wired into the live PRODUCTION run path, so a real CLI run dispatched the not-implemented STUBs for all but read_file. The CommandRunner seam was injected but discarded (void). The budget guard was also left to the IF-011 default (deps.maxIterations/tokenBudget absent), so production runs ignored Config ceilings.
Action    : Rewired composeAndRun (src/cli.ts) to construct ALL FIVE real executors: createSearchTool/createWriteEditTool/createRunCommandTool/createApplyPatchTool with their seam deps (path-sandbox over Config.root; ApprovalGate with injected confirm/confirmCommand seams + transcript+runId; createAllowlist(Config.allowlist); CommandRunner injected; repoContext.testCommand for tests-as-signal), passed positionally to createToolRegistry(read,search,writeEdit,runCommand,applyPatch). Also constructed the real createBudgetController({maxIterations,tokenBudget}) from Config and passed it as agent-run's budget dep so production runs are bounded by the resolved ceilings. Added optional editMode/commandMode/confirm/confirmCommand to RunAutocoderOptions + RunCliInput, threaded through. Closes DRIFT-005/008/011/013. No contract change (RunOutcome/registry signatures unchanged).
Escalation: none (no requirement contradicted).
## DRIFT-021  (SLICE-10 / TASK-019, Builder)  — derived layer, auto-applied
Discovery : DRIFT-016 carryover: src/agent-run.ts emitted the FIRST two transcript entries (run-started, context-gathered) plus transcript.open OUTSIDE the run-lifecycle try/catch. Per ERR-014/RULE-010 a transcript write failure (TRANSCRIPT_WRITE_FAILED) must route to the unrecoverable-error path -> Failed (non-zero exit); but a fatal write failure on those first two appends would ESCAPE unclassified (thrown out of run() with no terminate/classify), inconsistent with every other write in the loop which IS classified. agent-run was out of the SLICE-8 boundary so the fix was deferred to SLICE-10.
Action    : Moved transcript.open + the run-started + context-gathered appends + the conversation/toolSchemas setup INSIDE the existing run-lifecycle try block (src/agent-run.ts run()), and hoisted the BudgetController + terminalSignal construction ABOVE the try so the catch's terminate() closure can classify a failure on those first entries. A fatal TRANSCRIPT_WRITE_FAILED on the first appends now flows through the same catch -> isUserAbort? no -> unrecoverable-error -> budget.classify -> Failed/exit 1, consistent with the rest of the loop. No behavior change on the happy path; all 123 prior tests stay green. No contract change.
Escalation: none (no requirement contradicted).
## DRIFT-022  (SLICE-10 / TASK-019, Builder)  — derived layer, auto-applied
Discovery : The TASK-019 acceptance text says assert the RunOutcome (status, exitCode, filesChanged, testsResult). But the realized RunOutcome contract (src/contracts.ts / IF-016 split) is intentionally MINIMAL {status,exitCode,runId}; filesChanged + testsResult live in the IF-016 RunSummary (built by the reporter), which composeAndRun does not currently surface back as a return value (it returns RunOutcome). The agent-run loop emits the per-change diffs (edit-proposed/edit-applied) and the tests-as-signal (tests-run {passed,exitCode}) into the TRANSCRIPT, which is the inspectable audit surface 08-test-strategy names for the closed-loop e2e.
Action    : Asserted status+exitCode+runId from the returned RunOutcome AND asserted filesChanged (2 edit-applied + 2 edit-proposed each carrying a non-empty unified diff) + testsResult (2 tests-run rows: passed:false/exitCode:1 then passed:true/exitCode:0, in strict seq order) from the parsed Transcript — exactly the inspectable surface the test strategy specifies. No contract extension (RunOutcome left minimal; integration-only slice, no new production component). Documented choice; no 07-contracts.md edit.
Escalation: none (no requirement contradicted).
## DRIFT-023  (Stage 11 / finalization, Builder)  — derived layer, auto-applied
Discovery : DEFAULT_MODEL was the placeholder 'claude-sonnet-latest' (not a real Anthropic model id) in src/cli.ts and src/config.ts; behind the stubbed LLM seam so no test pinned it.
Action    : Set DEFAULT_MODEL to the real current id 'claude-sonnet-4-6' in both files; 128 tests + typecheck still green. Non-blocking polish for the flagship example.
Escalation: none (no requirement contradicted).
