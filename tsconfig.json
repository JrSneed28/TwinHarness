{
  "compilerOptions": {
    "target": "ES2022",
    "module": "CommonJS",
    "moduleResolution": "Node",
    "lib": ["ES2022"],
    "outDir": "dist",
    "rootDir": "src",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "declaration": false,
    "sourceMap": false,
    "resolveJsonModule": true,
    "forceConsistentCasingInFileNames": true
  },
  "include": ["src/**/*.ts"],
  "//": "M-1: src/mcp-server.ts is EXCLUDED from tsc EMIT so a bare `tsc` can never overwrite the esbuild bundle (dist/mcp-server.js) with un-shippable CJS that fails the bundle-purity guard and ships an MCP server that crashes ERR_MODULE_NOT_FOUND. esbuild (`npm run build:mcp`) is the SOLE producer of that file. Typecheck still covers mcp-server.ts via tsconfig.typecheck.json.",
  "exclude": ["node_modules", "dist", "tests", "src/mcp-server.ts"]
}
